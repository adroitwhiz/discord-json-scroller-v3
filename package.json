{
  "private": true,
  "name": "discord-json-scroller-v2",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && sirv build --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-plugin-react": "^7.19.0",
    "if-env": "^1.0.0",
    "preact-cli": "^3.0.0-rc.6",
    "sass": "^1.26.2",
    "sass-loader": "^8.0.2",
    "sirv-cli": "^0.4.5",
    "unistore": "^3.5.1"
  },
  "dependencies": {
    "jszip": "^3.2.2",
    "lodash.throttle": "^4.1.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
